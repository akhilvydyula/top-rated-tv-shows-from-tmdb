# Copy to Makefile.local and adjust. Makefile includes Makefile.local automatically.
#
# PYTHON := python3
# VENV := .venv
# DATA := data/my_kaggle_export.csv
# MODEL_OUT := models
# MLFLOW_TRACKING_URI := http://127.0.0.1:5000
# MLFLOW_EXPERIMENT := tmdb-tv-rating
# HOST := 0.0.0.0
# PORT := 8000
# UVICORN_RELOAD := --reload
